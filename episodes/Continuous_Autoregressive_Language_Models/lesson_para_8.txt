第8パラグラフです。

Experimentally, the main result is a better performance-compute trade-off.
---
日本語訳：実験的には、主な結果は、性能と計算量のより良いトレードオフが得られることです。  
補足：performance-compute trade-off は、「精度や性能」と「必要な計算資源」のバランスを指す、論文で非常によく出る表現です。

On WikiText-103, a CALM model with K equals 4 matches or approaches strong Transformer baselines at substantially lower FLOPs.
---
日本語訳：WikiText-103 では、K が 4 の CALM モデルは、大幅に少ない FLOPs で、強力な Transformer ベースラインと同等、あるいはそれに近い性能を示します。  
補足：baseline は比較対象となる標準的手法、FLOPs は計算量の代表的な指標です。 substantially lower は「かなり少ない」という学術的によく使われる強調表現です。

For example, CALM-M, at 371 million parameters, gets BrierLM close to a 281 million parameter Transformer-S while using 44 percent fewer training FLOPs and 34 percent fewer inference FLOPs.
---
日本語訳：例えば、3億7100万パラメータの CALM-M は、2億8100万パラメータの Transformer-S に近い BrierLM を達成しつつ、学習時の FLOPs を 44 パーセント、推論時の FLOPs を 34 パーセント削減しています。  
補足：gets BrierLM close to ... は「…に近い BrierLM を達成する」という意味です。 while using ... fewer FLOPs は、性能比較と計算効率を同時に述べる論文らしい構文です。

Larger CALM models also scale well.
---
日本語訳：より大規模な CALM モデルでも、スケーリング特性は良好です。  
補足：scale well は、モデルサイズ拡大に伴って性能が素直に伸びることを示す簡潔で重要な表現です。

Varying K shows the main thesis clearly: K equals 1 underperforms standard discrete modeling, but increasing semantic bandwidth to K equals 2 and especially K equals 4 shifts the frontier favorably.
---
日本語訳：K を変化させると、この研究の中心的主張が明確に示されます。K が 1 では標準的な離散モデリングより性能が劣りますが、semantic bandwidth を K が 2、特に K が 4 へ増やすと、性能と計算量のフロンティアが有利な方向へ移動します。  
補足：underperforms は「性能で劣る」、shifts the frontier favorably は「パレートフロンティアのような性能限界が望ましい方向に動く」という含みを持つ表現です。 semantic bandwidth は、この手法で表現できる意味情報の量を指しています。

K equals 8 hurts quality more, suggesting current models may lack the capacity to exploit that much compression.
---
日本語訳：K が 8 になると品質の悪化がより大きくなり、現在のモデルには、その程度の強い圧縮を活用しきるだけの容量がない可能性が示唆されます。  
補足：suggesting ... は、結果から控えめに解釈を述べる論文で頻出の言い回しです。 exploit compression は「圧縮をうまく使いこなす」という意味です。
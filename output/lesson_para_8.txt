第8パラグラフです。

Experimentally, the main result is improved performance per unit compute.
---
日本語訳：実験的には、主な結果は、計算資源あたりの性能が向上したことです。  
補足：performance per unit compute は、同じ計算量でより高性能、あるいは同じ性能をより少ない計算量で達成することを指す、効率性評価の重要表現です。

On WikiText-103, with training on the uncopyrighted Pile, CALM with K equals 4 beats the standard Transformer performance-compute frontier.
---
日本語訳：WikiText-103 において、著作権上問題のない Pile で学習した場合、K が 4 の CALM は標準的な Transformer の性能-計算量フロンティアを上回ります。  
補足：performance-compute frontier は、性能と計算コストの最良なトレードオフ曲線を指します。beats the frontier は、その既存の最良水準を超えるという意味です。

For example, CALM-M at 371 million parameters gets close to Transformer-S at 281 million, but uses 44 percent fewer training FLOPs and 34 percent fewer inference FLOPs.
---
日本語訳：例えば、3億7100万パラメータの CALM-M は、2億8100万パラメータの Transformer-S に近い性能を達成しつつ、学習時の FLOPs を 44 パーセント、推論時の FLOPs を 34 パーセント削減します。  
補足：gets close to は「ほぼ同等の性能に迫る」という比較表現です。training FLOPs と inference FLOPs を分けて示している点は、学習効率と推論効率を別々に評価していることを表します。

CALM-L with increasing K shows the central trade-off clearly: K equals 1 is worse than discrete baselines, meaning continuous prediction itself is harder; K equals 2 nearly halves cost with only a small loss; K equals 4 crosses into a better compute-performance regime; K equals 8 degrades, likely because model capacity is insufficient for that bandwidth.
---
日本語訳：K を増やした CALM-L は、中心的なトレードオフを明確に示しています。K が 1 の場合は離散的なベースラインより悪く、これは連続予測そのものがより難しいことを意味します。K が 2 では、わずかな性能低下だけでコストをほぼ半減します。K が 4 では、より良い計算量-性能の領域に入ります。K が 8 では性能が悪化しており、その帯域に対してモデル容量が不十分である可能性が高いです。  
補足：discrete baselines は通常の離散トークン予測モデルを指します。compute-performance regime は、ある計算条件下での性能特性の領域という意味です。bandwidth はここでは一度に扱う予測量・情報量の大きさとして読むと理解しやすいです。